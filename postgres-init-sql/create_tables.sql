CREATE TABLE IF NOT EXISTS tb_Name (
                        cName VARCHAR(50),
                        cNickname VARCHAR(20),
			PRIMARY KEY (cName)
                        );

CREATE TABLE IF NOT EXISTS tb_Research (
                        cName VARCHAR(50),
                        cResearch VARCHAR(50),
			PRIMARY KEY (cNAME, cResearch)
			);
